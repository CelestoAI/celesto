# Boot-smoke the Celesto-built universal kernel against published rootfs
# images BEFORE we commit a new manifest version. Catches the class of
# bug where a Kconfig delta (or a kernel bump) silently breaks boot for
# one of the published images — otherwise only discovered by the first
# user trying `celesto create`.
#
# Each cell tests a *real* published Celesto image (including the Alpine
# OpenCode image), exactly how the runtime boots them:
#   - Downloads the published kernel + ``<rootfs>-<arch>-rootfs.ext4.zst``
#     from the release, verifies the rootfs against the bundled manifest,
#     and decompresses it.
#   - Boots qemu-system-<arch> with the kernel + rootfs as virtio-blk,
#     using the production boot args: ``init=/init`` (every Celesto image
#     boots /init, not systemd) and ``root=/dev/vda`` (single ext4). The
#     SSH key is injected on the kernel cmdline via
#     ``celesto.authorized_key_b64`` — the same mechanism /init uses in
#     production. ``ip=`` matches the user-net subnet so the host port
#     forward reaches the guest.
#   - Waits for sshd, then logs in and asserts the vsock guest agent is
#     baked in and running.
#
# accel note: GH-hosted x86 runners expose /dev/kvm; the arm64 runners do
# not always. We use KVM when /dev/kvm is present and fall back to TCG
# (software emulation) otherwise, with a longer readiness budget. macOS
# HVF can't be exercised in CI (no nested virt on hosted macOS runners).

name: Smoke Published Images

on:
  workflow_dispatch:
    inputs:
      release_tag:
        description: >-
          Release tag to smoke-test (e.g. images-2026.06.23.0). Leave empty
          to use IMAGES_RELEASE_TAG from published.py.
        type: string
        default: ""
  workflow_run:
    # NB: these are matched by the upstream workflow's `name:` field (the
    # display name), NOT the YAML filename. Keep them in sync with
    # build-published-images.yml (`name: Build Published Images`). Renaming
    # that workflow's display name without updating this list silently disables
    # the smoke gate. Kernel-only builds do not produce rootfs assets, so they
    # cannot trigger this workflow.
    workflows: ["Build Published Images"]
    types:
      - completed
    branches:
      - main

permissions:
  # Draft release assets require an authenticated token with repository write
  # access; otherwise `gh release download` reports "release not found".
  contents: write

jobs:
  smoke:
    # Only run after a successful build — failed builds left the assets in
    # an unknown state.
    if: ${{ github.event_name == 'workflow_dispatch' || github.event.workflow_run.conclusion == 'success' }}
    strategy:
      fail-fast: false
      matrix:
        arch: [amd64, arm64]
        rootfs: [openclaw, opencode, ubuntu, linux-desktop]
        os: [ubuntu, alpine]
        exclude:
          - rootfs: openclaw
            os: alpine
          - rootfs: ubuntu
            os: alpine
          - rootfs: linux-desktop
            os: alpine
    runs-on: ${{ matrix.arch == 'arm64' && 'ubuntu-24.04-arm' || 'ubuntu-latest' }}
    timeout-minutes: 20

    steps:
      - name: Check out repository
        uses: actions/checkout@v4
        with:
          persist-credentials: false

      - name: Install QEMU + tooling
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y --no-install-recommends \
              qemu-system-x86 qemu-system-arm qemu-utils \
              zstd openssh-client
          # KVM access — GH runners ship /dev/kvm but the runner user isn't
          # always in the kvm group. Add it; sudo on the qemu invocation also
          # works as a fallback.
          sudo usermod -a -G kvm $USER || true
          sudo modprobe vhost_vsock || echo "vhost_vsock unavailable"
          sudo chgrp kvm /dev/vhost-vsock || true
          sudo chmod 660 /dev/vhost-vsock || true

      - name: Resolve release tag
        id: release
        env:
          INPUT_TAG: ${{ inputs.release_tag }}
        run: |
          if [ -n "${INPUT_TAG:-}" ]; then
            tag="$INPUT_TAG"
          else
            # Single source of truth — published.py::IMAGES_RELEASE_TAG.
            tag=$(awk -F'"' '/^IMAGES_RELEASE_TAG[[:space:]]*=/{print $2; exit}' src/celesto/images/published.py)
          fi
          if [ -z "$tag" ]; then
            echo "ERROR: IMAGES_RELEASE_TAG could not be parsed from src/celesto/images/published.py" >&2
            exit 1
          fi
          echo "tag=${tag}" >> "$GITHUB_OUTPUT"

      - name: Download kernel
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAG: ${{ steps.release.outputs.tag }}
          ARCH: ${{ matrix.arch }}
        run: |
          mkdir -p smoke
          gh release download "$TAG" \
              --repo "$GITHUB_REPOSITORY" \
              --pattern "vmlinux-${ARCH}.image" \
              -D smoke

      - name: Download rootfs
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAG: ${{ steps.release.outputs.tag }}
          ARCH: ${{ matrix.arch }}
          ROOTFS: ${{ matrix.rootfs }}
          OS: ${{ matrix.os }}
        run: |
          # These are published Celesto raw-ext4 images — same download and
          # decompress path for every preset.
          suffix=""
          if [ "$OS" = "alpine" ]; then
            suffix="-alpine"
          fi
          asset="${ROOTFS}-${ARCH}${suffix}-rootfs.ext4.zst"
          gh release download "$TAG" \
              --repo "$GITHUB_REPOSITORY" \
              --pattern "$asset" \
              -D smoke
          release_digest=$(gh release view "$TAG" \
              --repo "$GITHUB_REPOSITORY" \
              --json assets \
              --jq ".assets[] | select(.name == \"$asset\") | .digest")
          release_sha=${release_digest#sha256:}
          actual_sha=$(sha256sum "smoke/$asset" | cut -d' ' -f1)
          if [ -z "$release_sha" ] || [ "$actual_sha" != "$release_sha" ]; then
            echo "Published checksum mismatch for $asset" >&2
            exit 1
          fi

          preset_symbol=$(printf '%s' "$ROOTFS" | tr '[:lower:]-' '[:upper:]_')
          if [ "$OS" = "alpine" ]; then
            symbol="_${preset_symbol}_${ARCH^^}_ALPINE_ROOTFS_SHA"
          else
            symbol="_${preset_symbol}_${ARCH^^}_ROOTFS_SHA"
          fi
          manifest_sha=$(awk -v symbol="$symbol" '$1 == symbol {gsub(/"/, "", $3); print $3}' src/celesto/images/published.py)
          if [ -n "$manifest_sha" ] && [ "$actual_sha" != "$manifest_sha" ]; then
            echo "Manifest checksum mismatch for $asset" >&2
            exit 1
          fi
          zstd -d "smoke/$asset" -o smoke/rootfs.raw
          rm "smoke/$asset"

      - name: Generate SSH key
        run: |
          # Injected into the guest on the kernel cmdline (base64 of the pubkey
          # line) — the same celesto.authorized_key_b64 mechanism /init uses.
          ssh-keygen -t ed25519 -N "" -f smoke/id_ed25519 -C smoke
          echo "AUTHKEY_B64=$(base64 -w0 smoke/id_ed25519.pub)" >> "$GITHUB_ENV"

      - name: Boot the VM, wait for SSH, assert guest agent
        env:
          ARCH: ${{ matrix.arch }}
          ROOTFS: ${{ matrix.rootfs }}
          OS: ${{ matrix.os }}
        run: |
          set -euo pipefail
          # KVM when the runner exposes it, else TCG software emulation with a
          # larger readiness budget (arm64 GH runners don't always ship /dev/kvm).
          if [ -e /dev/kvm ]; then
            ACCEL=kvm; CPU="-cpu host"; BUDGET=120
          else
            ACCEL=tcg; CPU="-cpu max"; BUDGET=420
            echo "/dev/kvm unavailable — falling back to TCG (slower); budget=${BUDGET}s"
          fi
          if [ "$ARCH" = "amd64" ]; then
            QEMU=qemu-system-x86_64
            MACHINE="-machine pc-q35-4.2,accel=${ACCEL}"
            VSOCK_DEVICE="vhost-vsock-pci"
            CONSOLE=ttyS0
          else
            QEMU=qemu-system-aarch64
            MACHINE="-machine virt,accel=${ACCEL}"
            VSOCK_DEVICE="vhost-vsock-device"
            CONSOLE=ttyAMA0
          fi
          # Production boot recipe for every Celesto image: init=/init (not
          # systemd), single-ext4 root=/dev/vda, the SSH key on the cmdline, and
          # an ip= matching QEMU user-net (10.0.2.0/24) so the host port forward
          # reaches the guest's static IP.
          GUEST_IP=10.0.2.15
          APPEND="console=${CONSOLE} reboot=k panic=1 root=/dev/vda rw init=/init"
          APPEND="${APPEND} ip=${GUEST_IP}::10.0.2.2:255.255.255.0::eth0:off"
          APPEND="${APPEND} celesto.authorized_key_b64=${AUTHKEY_B64}"
          sudo $QEMU \
              $MACHINE $CPU -smp 2 -m 2048 \
              -kernel smoke/vmlinux-${ARCH}.image \
              -drive file=smoke/rootfs.raw,if=none,id=root,format=raw \
              -device virtio-blk-pci,drive=root \
              -netdev user,id=net0,hostfwd=tcp:127.0.0.1:2223-${GUEST_IP}:22 \
              -device virtio-net-pci,netdev=net0 \
              -device "${VSOCK_DEVICE},guest-cid=1024" \
              -append "${APPEND}" \
              -nographic -no-reboot \
              > smoke/boot.log 2>&1 &
          QPID=$!
          ok=0
          for i in $(seq 1 "$BUDGET"); do
            sleep 1
            if ssh-keyscan -T 2 -p 2223 127.0.0.1 2>/dev/null | grep -q "ssh-"; then
              echo "sshd ready after ${i}s"
              ok=1
              break
            fi
          done
          agent_ok=0
          if [ "$ok" -eq 1 ]; then
            # Boot proved kernel + rootfs + /init; now confirm the vsock guest
            # agent is baked in and running — the thing published images ship.
            if ssh -p 2223 -i smoke/id_ed25519 \
                   -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \
                   -o ConnectTimeout=10 root@127.0.0.1 \
                   'test -x /usr/local/bin/celesto-guest-agent && pgrep -f celesto-guest-agent >/dev/null'; then
              echo "guest agent present and running"
              agent_ok=1
            fi
            if [ "$ROOTFS" = "linux-desktop" ] && [ "$agent_ok" -eq 1 ]; then
              if ssh -p 2223 -i smoke/id_ed25519 \
                     -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \
                     -o ConnectTimeout=10 root@127.0.0.1 \
                     'set -eu
                      test -x /usr/local/bin/celesto-browser-session
                      command -v chromium >/dev/null
                      command -v openbox >/dev/null
                      command -v tint2 >/dev/null
                      command -v lxterminal >/dev/null
                      command -v pcmanfm >/dev/null
                      command -v mousepad >/dev/null
                      /usr/local/bin/celesto-browser-session start computer 1280 720 9222 6080 /opt/celesto-browser/profiles/smoke /opt/celesto-browser/downloads/smoke 0 1 /opt/celesto-browser/artifacts/smoke
                      for _attempt in $(seq 1 60); do
                        if pgrep -x Xvfb >/dev/null &&
                           pgrep -x openbox >/dev/null &&
                           pgrep -x tint2 >/dev/null &&
                           pgrep -x x11vnc >/dev/null &&
                           pgrep -f websockify >/dev/null &&
                           pgrep -f chromium >/dev/null &&
                           python3 -c '\''import urllib.request; urllib.request.urlopen("http://127.0.0.1:6080/vnc.html", timeout=2).read()'\'' &&
                           python3 -c '\''import urllib.request; urllib.request.urlopen("http://127.0.0.1:9222/json/version", timeout=2).read()'\''; then
                          exit 0
                        fi
                        sleep 1
                      done
                      exit 1'; then
                echo "Linux desktop applications and services are ready"
              else
                echo "Linux desktop applications or services did not become ready"
                agent_ok=0
              fi
            fi
          fi
          sudo kill "$QPID" 2>/dev/null || true
          sleep 2
          sudo kill -9 "$QPID" 2>/dev/null || true
          echo "--- last 80 lines of boot log ---"
          tail -80 smoke/boot.log
          if [ "$ok" -ne 1 ]; then
            echo "::error::sshd did not become ready within ${BUDGET}s on ${ARCH}/${OS}/${ROOTFS}"
            exit 1
          fi
          if [ "$agent_ok" -ne 1 ]; then
            echo "::error::guest agent missing or not running on ${ARCH}/${OS}/${ROOTFS}"
            exit 1
          fi

      - name: Step summary
        if: always()
        env:
          ARCH: ${{ matrix.arch }}
          ROOTFS: ${{ matrix.rootfs }}
          TAG: ${{ steps.release.outputs.tag }}
        run: |
          {
            echo "## Smoke: \`${ARCH}\` × \`${ROOTFS}\` from \`${TAG}\`"
            echo ""
            echo "Status: ${{ job.status }}"
          } >> "$GITHUB_STEP_SUMMARY"
