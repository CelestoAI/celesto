[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "celesto"
version = "0.0.15a0"
description = "Sandbox for computer-using agents with a unified API for VMMs — Firecracker, QEMU, and libkrun."
readme = "README.md"
license = "Apache-2.0"
requires-python = ">=3.11"
authors = [
    { name = "Celesto AI", email = "oss@celesto.ai" }
]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: Apache Software License",
    "Operating System :: MacOS :: MacOS X",
    "Operating System :: POSIX :: Linux",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
    "Programming Language :: Rust",
    "Topic :: System :: Emulators",
]
dependencies = [
    "attrs>=23.2",
    "httpx>=0.27,<0.29",
    "python-dateutil>=2.8",
    "typing-extensions>=4.10",
    "websockets>=14.0",
    "click>=8.0",
    "smolvm-core~=2026.6.24",
    "paramiko>=3.0",
    "pycdlib>=1.14.0",
    "pydantic>=2.0",
    "rich>=13.0",
    "requests>=2.34.2",
    # Decompresses zstd-compressed published images (downloaded via
    # ensure_published_image). Pure C extension, ~200 KB wheel, no system deps.
    "zstandard>=0.22",
]

[project.urls]
Homepage = "https://celesto.ai"
Documentation = "https://docs.celesto.ai"
Repository = "https://github.com/CelestoAI/celesto"
Issues = "https://github.com/CelestoAI/celesto/issues"
Changelog = "https://github.com/CelestoAI/celesto/releases"

[project.optional-dependencies]
dev = [
    "pytest>=7.0",
    "pytest-asyncio>=1.4.0",
    "pytest-cov>=4.0",
    "ruff>=0.15.0",
    "mypy>=1.0",
    "pre-commit>=4.2.0",
]
s3 = [
    "boto3>=1.43.96",
    "python-dotenv>=1.0",
]
dashboard = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.34.0",
]
server = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.34.0",
]
all = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.34.0",
]

[project.scripts]
celesto = "celesto.cli.main:main"

[tool.hatch.build.targets.wheel]
packages = ["src/celesto", "src/_celesto_cloud_api"]

[tool.hatch.build.targets.wheel.force-include]
"scripts/system-setup.sh" = "celesto/_setup_assets/system-setup.sh"
"scripts/system-setup-macos.sh" = "celesto/_setup_assets/system-setup-macos.sh"
"scripts/internal/install-firecracker.sh" = "celesto/_setup_assets/internal/install-firecracker.sh"
"scripts/internal/configure-runtime-sudoers.sh" = "celesto/_setup_assets/internal/configure-runtime-sudoers.sh"
"scripts/internal/image-build-loopfs.sh" = "celesto/_setup_assets/internal/image-build-loopfs.sh"
"THIRD_PARTY_NOTICES.md" = "celesto/THIRD_PARTY_NOTICES.md"

[tool.hatch.build]
exclude = ["/ui", "/smolvm-core"]

[tool.ruff]
extend-exclude = ["src/_celesto_cloud_api"]
target-version = "py311"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "N", "W", "UP", "B", "C4", "SIM"]
# Migrating `(str, Enum)` to `StrEnum` changes `str()`/f-string output from
# "VMState.RUNNING" to "running", which would alter CLI output and JSON payloads.
ignore = ["UP042"]

[tool.mypy]
python_version = "3.11"
strict = true
warn_return_any = true
warn_unused_ignores = true

[tool.uv.sources]
smolvm-core = { workspace = true }

[tool.uv.workspace]
members = ["smolvm-core"]

[tool.pytest.ini_options]
testpaths = ["tests"]
# e2e tests boot real KVM VMs; deselect them from the default fast suite.
# The e2e workflow opts in explicitly with `pytest -m e2e`.
addopts = "-v --tb=short -m 'not e2e'"
markers = [
    "e2e: boots real KVM VMs; CI/explicit only, needs /dev/kvm",
]
