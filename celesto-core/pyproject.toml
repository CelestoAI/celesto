[build-system]
requires = ["maturin>=1.7,<2.0"]
build-backend = "maturin"

[project]
name = "celesto-core"
dynamic = ["version"]
description = "Rust-accelerated runtime and network operations for Celesto"
readme = "README.md"
license = "Apache-2.0"
requires-python = ">=3.11"
authors = [
    { name = "Celesto AI", email = "hello@celesto.ai" }
]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: Apache Software License",
    "Operating System :: POSIX :: Linux",
    "Operating System :: MacOS :: MacOS X",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Rust",
]

[project.urls]
Homepage = "https://github.com/CelestoAI/Celesto"
Repository = "https://github.com/CelestoAI/Celesto"
Documentation = "https://docs.celesto.ai"

[tool.maturin]
features = ["extension-module"]
python-source = "python"
module-name = "celesto_core._ffi"
